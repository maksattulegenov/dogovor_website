# 🔐 Google Drive Authentication Guide

## Проблема: "Google Drive не авторизован"

Если вы видите ошибку при сохранении подписи, это означает, что нужно выполнить одноразовую авторизацию Google Drive.

## Решение (5 простых шагов):

### 1️⃣ Найдите URL авторизации
Посмотрите в консоль сервера (где запущен `npm start`). Там будет ссылка вида:
```
⚠️  Google Drive not authenticated!
Visit this URL to authenticate:
https://accounts.google.com/o/oauth2/v2/auth?access_type=...
```

### 2️⃣ Скопируйте ссылку
Скопируйте **всю** ссылку из консоли (она может быть разбита на несколько строк)

### 3️⃣ Откройте ссылку в браузере
Вставьте ссылку в адресную строку браузера

### 4️⃣ Войдите и разрешите доступ
- Войдите в свой Google аккаунт
- Нажмите "Разрешить" / "Allow"
- Дайте доступ к Google Drive

### 5️⃣ Готово!
После успешной авторизации:
- Закройте вкладку браузера
- Вернитесь на сайт http://localhost:3000
- Попробуйте сохранить подпись снова

## 💡 Важно:
- Авторизация нужна **только один раз**
- После авторизации создастся файл `tokens.json`
- В следующий раз всё будет работать автоматически

## 🔍 Проверка статуса:
- Откройте консоль браузера (F12)
- Там будет сообщение, если требуется авторизация
- Ссылка для авторизации будет в консоли

## ❓ Если не работает:
1. Убедитесь, что сервер запущен (`npm start`)
2. Проверьте, что сайт открыт на http://localhost:3000
3. Проверьте консоль сервера для сообщений об ошибках
